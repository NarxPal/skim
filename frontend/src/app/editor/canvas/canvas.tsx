import React from "react";
import styles from "@/styles/canvas.module.css";

const Canvas = () => {
  return (
    <div className={styles.canvas}>
      <div className={styles.canvas_content}></div>
    </div>
  );
};

export default Canvas;
